
# 数据

数据都是去重过的。pos与neg，labeled与unlabeled，都是不重合的。

# 任务描述

我在做一个序列分类任务。任务描述如下：  
【1】输入数据格式：正则表达式"(C{3,}[ATCG]{1,12}){3}C{3,}"匹配的碱基序列，和每个字符串对应的0-1标签。在输入碱基序列中，连续的三个或三个以上的C碱基被认作一个 C-tract。C-tract之间的序列称作 spacer。  
【2】数据特点：一个输入碱基序列中，C-tract 的数量不固定，但是只有 4 个 C-tract 有效，其余 C-tract 实际上也是 spacer的一部分。而这 4 个真的 C-tract 会13一组，24一组，的组成两对C-tract配对 (半质子化的C-C碱基对)。也就是说，一个 C-tract 绝对不会和相邻的 C-tract 配对。  
【3】Tokenizer: (1) 第一个 Tokenizer: 不区分碱基是否属于 C-tract 的一部分，只是将每个碱基映射为一个 token，也就是说除了特殊 tokens 外，整个词表里只有 [A，T，C，G] 这四种词。(2) 将碱基序列的 C-tract 和 spacer 用 BPE 的方式分词，在 C-tract 和 spacer 之间添加空格，然后用 pre_tokenizers.Whitespace() 和 BpeTrainer() 去训练一个 BPE tokenizer，此 tokenizer 的词表中包含不同长度的 C 碱基序列作为代表 C-tract 的 tokens，还有四种碱基组成的序列作为代表 spacer 的 tokens。  
【4】模型架构：我用 transformers 库的 RobertaForSequenceClassification 模型。并基于上面两个 Tokenizer 分别训练了两个模型。我发现这两个模型性能相近。

# 本方法进步点：
1. 除了传统手动提取的特征外，可以进一步利用到序列本身的特征，比如 C-tract 的数量。前人的方法用一定的规则提取了 4 个 C-tract 和 3 个 spacer。但其实这段序列中可能 C-tract 约多越好，因为这样会有更多的潜在形态可供构成 i-motif。而我这个模型的输入满足了考虑这个问题的条件。
2. 前人固定的提取了序列的特征，构建了不平衡的数据集 (比如 iM-Seeker 的 "iM-Seeker predicts iM structure folding status" section)。比如其中有 70 多万的负例，而只有 八九千 的正例。而我用半监督学习的方法，可以将正例从 一万多 补充到 两万左右，同时保持正负例数量一比一，又提高了模型的性能。
3. 在模型结构上，我用了双塔模型，这样可以更好地利用到两个 Tokenizer 的信息（如 base 和 BPE，能够同时处理低粒度（如字符级）和高粒度（如词级）特征）。并且我还尝试了交叉注意力的方法，让两个模型的不同颗粒度的特征之间实现信息的交换。这样可以更好地帮助模型在不同尺度上分析输入序列的信息。
4. 在训练方法上，我分别训练了两个 Tokenizer 对应的模型，然后将两个模型的最优参数加载到双塔模型中，再训练。  
base 和 bpe 作为双塔模型的两个塔，可能由于输入张量大小不同，他们独自训练时在不同的周期达到最优。在双塔里一起训练时，可能会互相形成干扰，无法同时达到最优。不同周期下，由于模型训练进程不同步会有木桶的短板。所以先加载单模型训练的最优参数，再训练。  
结果表明确实提升了性能。比v2的加载预训练掩码模型的效果好。

# 记录模型优化的关键节点

## 基于 BPE 分词的模型
1. 使用空格分隔，然后用 BPE 分词。
2. 使用 BATCH_SIZE = 1024

## 基于 单个BASE 分词的模型

## 双塔模型

初步的思路是：  

### 方案一：交叉注意力
交叉注意力 (Cross-attention)：在每一层 Transformer 中，通过跨层注意力机制让两个模型的特征相互影响，从而实现信息的交换。

#### Step 1
设输入序列 X 经过两个不同的 Tokenizer 分别处理，得到两个表示：

X_1：来自 Tokenizer 1（普通碱基表示），大小为 [batch_size, sequence_length_1]。
X_2：来自 Tokenizer 2（C-tract 和 spacer 分割表示），大小为 [batch_size, sequence_length_2]。
每个输入将被分别传入到两个独立的模型中进行初步的编码，得到两个特征表示：

Z_1 = Encoder_1(X_1)，其中 Encoder_1 是第一个模型的编码器。
Z_2 = Encoder_2(X_2)，其中 Encoder_2 是第二个模型的编码器。

在后续的 Transformer 层中，我们可以设计一个交叉注意力机制，让 Z_1 和 Z_2 之间进行信息交换。具体地，我们可以在每一层的 Self-Attention 和 Feed-Forward 层之后添加一个交叉注意力层，类似 Transformer Decoder Layer。

#### Step 2
在每个 Transformer Decoder 层中，进行如下两步操作：

1. 用 Z_2 更新 Z_1
- Query 来自 Z_1。
- Key 和 Value 来自 Z_2。
- 交叉注意力计算后更新 Z_1，记为 Z_1'：$Z_1' = CrossAttention(Z_1, Z_2)$

2. 用 Z_1 更新 Z_2
- Query 来自 Z_2。
- Key 和 Value 来自 Z_1。
- 交叉注意力计算后更新 Z_2，记为 Z_2'：$Z_2' = CrossAttention(Z_2, Z_1)$
通过这两个步骤，Z_1 和 Z_2 会在当前层中互相交换信息，并融合各自的特征。

#### Step 3
递归更新
将更新后的 Z_1' 和 Z_2' 继续输入到各自的模型的下一层 Transformer 中，重复上述交叉注意力过程，形成逐层融合。

## 优化方案
一开始用双塔默认参数从头训练，逐步做了以下优化：

【1】修改输出层 (SAVED_MODEL_DIR_3D8H256DRC2 路径)。  
发现一：仅输出 "\<s>" 维度，有效果提升。

【2】用四种不同方式加载预训练参数 (DualTowerForSequenceClassification 类中注释)。  

发现一：(load_pretrained_weights_v2)  
给没有模型对应参数的 cross-attention 加载另外一个塔的 self-attention 参数，可以提升性能。  

发现二：(load_pretrained_weights_v4，imotifTower_3D8H256D_P4)    
尝试直接将 base 和 bpe 两个模型最优的结果的参数加载，然后训练。  
> 理由: base 和 bpe 作为双塔模型的两个塔，他们独自训练时在不同的周期达到最优.在双塔里一起训练时，可能无法同时达到最优，会互相形成干扰，不同周期由于模型训练进程不同步会有木桶的短板。所以先加载单模型训练的最优参数，再训练，看看是否在两个塔都是最优的情况下，双塔模型效果会更好？  
结果：确实提升了性能。比v2的加载预训练掩码模型的效果好。

发现三：(load_pretrained_weights_v3)  
构建半监督学习，标注伪标签，然后在 imotifTower_3D8H256D_P4 验证损失最优的模型上进一步用伪标签进一步训练。

用第一种伪标签标注方案的结果和本体差不多。  
而用第二种伪标签标注方案的结果如下：

> 在使用固定的随机种子和相同模型结构的情况下，在验证集上，用第二种伪标签标注方案，模型效果验证损失从26.45%降低到24.85%，accuracy从92.21%提升到92.61%，AUROC从97.33%提升到97.56%，AUPRC从97.68%提升到97.81%，F1-score从92.04%提升到92.61%。

结果均比原来的模型和第一种伪标签标注方案的结果更好。对于已经表现非常优秀的模型（比如 accuracy 已经达到 92% 以上），进一步提升往往会变得越来越困难。因此，尽管每个单独指标的提升幅度不算巨大，但在综合评估下，这些提升是有说服力的。  
此外，这些指标的提升表明，伪标签方法有助于增强此任务的模型的泛化能力。

其他优点：
1. 并且由于是继续训练，消耗的资源也更少。
2. 补足了正反例数量不均衡的问题。
3. 召回率更高。

### 伪标签标注方案

首先，用 三个模型分别进行预测 得到三组对 HEK293T 的预测结果，
> 验证集不含有伪标签数据，和最开始的验证集相同。

【1】伪标签标注方案 1：

用阈值 $POS\_THRES=0.7958, NEG\_THRES=-0.2$ 筛选三个模型的结果均符合阈值的样本，此时获得正反例数量都是2923。  
然后基于之前原始数据集（训练集的pos samples同样需要upsample到与neg samples同数量），将这些样本加入到训练集中，基于imotifTower_3D8H256D_P4继续训练模型。

【2】伪标签标注方案 2：

用阈值 $POS\_THRES=0.2, NEG\_THRES=-0.5$ 正例样本只需是塔模型的预测满足阈值（三个模型同时满足的话，样本不够），而负例需三个模型均满足阈值，
生成 $pos\ num: 10276; neg\ num: 2137$ 的伪标签数据集。  
与 【1】 中的方案相比，这个方案多生成正例，有两点原因：
1. 原本未标注数据就是从三个 replicate 中获取的，只是这些数据没有在三个 replicate 中均出现，所以这些数据本来就更倾向于是正例。
2. 原来的已标注数据中，正例比负例=10966:22974，正例少了一万多，正好用伪标签正例来补充。

用伪标签正例+已标注正例组成正例，然后将 标注负例 downsampling 到 正例数量-伪标签负例数量 的数量，然后加上伪标签负例，实现总体正负一比一，基于 imotifTower_3D8H256D_P4 继续训练模型。


